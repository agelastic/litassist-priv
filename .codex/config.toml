personality = "friendly"

# Top model at maximum reasoning effort for this project's agent handoffs.
model = "gpt-5.5"
model_reasoning_effort = "xhigh"

[mcp_servers]

[mcp_servers.code-review-graph]
command = "uvx"
args = ["code-review-graph", "serve"]

[features]
hooks = true
