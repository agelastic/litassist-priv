"""
Utils module for LitAssist.

Submodules expose utilities directly; this package level does not re-export
anything to keep import cost low. Import from the relevant submodule, e.g.
``from litassist.utils.formatting import success_message``.
"""
